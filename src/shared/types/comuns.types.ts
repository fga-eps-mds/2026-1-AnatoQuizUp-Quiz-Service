// Utilitarios de tipo reutilizaveis pelo servico inteiro.

// Valor que tambem pode ser nulo.
export type Nullable<T> = T | null;

// Valor que tambem pode ser indefinido.
export type Optional<T> = T | undefined;
